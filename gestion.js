(() => { const cfg=window.DV_CONFIG||{}; const p=new URLSearchParams(location.search); const id=(p.get('id')||'').trim(); const code=(p.get('code')||'').trim(); const frame=document.querySelector('#management-frame'); const loading=document.querySelector('#management-loading'); if(!cfg.appsScriptUrl||!id||!code){loading.textContent='Faltan el identificador o el código de acceso.';return;} const q=new URLSearchParams({action:'portal',id,code,_:Date.now()}); frame.src=`${cfg.appsScriptUrl}?${q}`; frame.addEventListener('load',()=>loading.hidden=true); })();